Docker Overview (~10 min): https://docs.docker.com/get-started/overview/

**image** - template for running a container  
**container** - runnable instance of an image, isolated from other containers and host by default
```
docker pull
docker run
docker create
docker start
docker exec
```

